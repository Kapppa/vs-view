name: Publish
on:
  push:
    tags:
      - "*/v*"
  workflow_dispatch:
    inputs:
      package:
        description: "Package to build"
        required: true
        type: choice
        options:
          - vsview
          - vspackrgb
          - audio-convert
          - comp
          - fftspectrum
          - frameprops-extended
          - split-planes

env:
  PYTHON_VERSION: "3.14"

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.check.outputs.matrix }}
      is-vspackrgb: ${{ steps.check.outputs.is-vspackrgb }}
    steps:
      - uses: actions/checkout@v6
      - id: check
        run: python .github/scripts/resolve_package.py
        env:
          INPUT_PACKAGE: ${{ inputs.package }}

  # Pure Python packages
  python-package:
    name: ${{ matrix.package }}
    needs: [prepare]
    if: needs.prepare.outputs.matrix != '[]'
    runs-on: ubuntu-latest

    permissions:
      contents: read
      id-token: write

    strategy:
      matrix:
        include: ${{ fromJson(needs.prepare.outputs.matrix) }}

    environment:
      name: pypi-${{ matrix.package }}
      url: https://pypi.org/p/${{ matrix.package }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          enable-cache: true

      - name: Generate SBOM
        run: uv export --package "${{ matrix.package }}" --no-dev --format cyclonedx1.5 -o "${{ matrix.path }}/sboms.json"

      - name: Build
        run: uv build --package "${{ matrix.package }}" ${{ matrix.build_args }} --clear

      - name: Check the output
        run: uvx twine check --strict dist/*

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1

      - name: Post webhook for failure
        if: failure()
        uses: Jaded-Encoding-Thaumaturgy/discord-failure-notifier@v1
        with:
          webhook-url: ${{ secrets.WEBHOOK_URL }}

  # vspackrgb - Cython package, needs cross-platform wheel builds
  vspackrgb_build:
    name: Build vspackrgb wheels (${{ matrix.os }})
    needs: [prepare]
    # Simplified condition using prepare output
    if: needs.prepare.outputs.is-vspackrgb == 'true'
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, ubuntu-24.04-arm, windows-latest, macos-latest]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          enable-cache: true

      - name: Generate SBOM
        run: uv export --package vspackrgb --no-dev --format cyclonedx1.5 -o src/vspackrgb/sboms.json

      - name: Build wheels
        uses: pypa/cibuildwheel@v3.3
        with:
          package-dir: src/vspackrgb
          output-dir: wheelhouse

      - name: Upload wheels
        uses: actions/upload-artifact@v7
        with:
          name: wheels-${{ matrix.os }}
          path: wheelhouse/*.whl

  vspackrgb_sdist:
    name: Build vspackrgb sdist
    needs: [prepare]
    if: needs.prepare.outputs.is-vspackrgb == 'true'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          enable-cache: true

      - name: Build sdist
        run: uv build --package vspackrgb --sdist --out-dir dist --clear

      - name: Upload sdist
        uses: actions/upload-artifact@v7
        with:
          name: sdist
          path: dist/*.tar.gz

  # Publish vspackrgb after all builds complete
  vspackrgb_publish:
    name: Publish vspackrgb to PyPI
    runs-on: ubuntu-latest
    needs: [vspackrgb_build, vspackrgb_sdist]

    environment:
      name: pypi-vspackrgb
      url: https://pypi.org/p/vspackrgb

    permissions:
      contents: read
      id-token: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          merge-multiple: true

      - name: Set up uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          enable-cache: false

      - name: Check the output
        run: uvx twine check --strict dist/*

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1

      - name: Post webhook for failure
        if: failure()
        uses: Jaded-Encoding-Thaumaturgy/discord-failure-notifier@v1
        with:
          webhook-url: ${{ secrets.WEBHOOK_URL }}
